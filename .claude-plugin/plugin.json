{
  "name": "openl",
  "description": "Work with OpenL Studio from Codex or Claude Code — manage rules, projects, tables and tests over the OpenL MCP server.",
  "version": "0.2.0",
  "author": { "name": "OpenL Tablets" },
  "homepage": "https://github.com/openl-tablets/openl-ai-plugin",
  "keywords": ["openl", "openl-tablets", "rules-engine", "brms", "mcp"],
  "userConfig": {
    "studio_base_url": {
      "type": "string",
      "title": "OpenL Studio address",
      "description": "The address you open in your browser to use OpenL Studio, e.g. https://studio.example.com",
      "required": true
    },
    "studio_token": {
      "type": "string",
      "title": "Personal Access Token",
      "description": "Create one in OpenL Studio under User → Personal Access Tokens, then paste it here. This is how Claude signs in as you. Leave empty only for a single-user Studio that has no sign-in. Run /openl:connect and Claude will walk you through it.",
      "sensitive": true,
      "required": false
    }
  }
}
